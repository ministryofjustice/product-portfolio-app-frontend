apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product-portfolio-app-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "product-portfolio-app-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product-portfolio-app-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
