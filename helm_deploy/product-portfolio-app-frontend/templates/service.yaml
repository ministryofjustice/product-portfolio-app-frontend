apiVersion: v1
kind: Service
metadata:
  name: {{ include "product-portfolio-app-frontend.fullname" . }}
  labels:
    {{- include "product-portfolio-app-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "product-portfolio-app-frontend.selectorLabels" . | nindent 4 }}
